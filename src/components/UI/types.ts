export type ProductCardType = {
  name: string;
  volume: string | null;
  price: string | null;
  oldPrice: number | null;
  isSale: Boolean;
  isHit: Boolean;
  isNew: Boolean;
  reviews: number | null;
  image: string;
  amount: number;
  id: string;
};
